# Examples

Run the extracted helpers through the public package entrypoint:

```sh
dart run example/jocaagura_domain_core_example.dart
```

The example decodes representative order fields, converts a duration and carries
a typed `Unit` success value. It performs no backend operation or network call.
It does not claim that an existing application has migrated to this package.
